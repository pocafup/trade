# 项目说明
这是一个 Next.js + TypeScript 前端 (src/)。我要新增一个 Python (FastAPI)
量化计算服务，放在 quant/ 目录，通过 API 给前端提供股票风险评级和买卖信号。

# 我的背景
熟编程，统计一般。涉及统计/金融概念时，用注释解释清楚每个公式在算什么。

# 铁律（非常重要，必须遵守）
1. 任何信号/策略在我相信之前必须先回测，且严禁前视偏差(look-ahead bias)：
   计算第 T 天的信号只能用 T 天及之前的数据。
2. 回测必须扣除交易成本和滑点，必须做样本外验证。
3. 风险指标(波动率/回撤/Beta/VaR)是确定性计算，和"预测"严格分开，不要混在一起。
4. 每写一个计算函数，配一个单元测试验证数学正确性。
5. 不要引入"网上的分析/情绪/新闻"，只用价格和成交量数据。
6. 所有股票价格必须只通过 quant/data/prices.py 的 fetch() 获取(复权 SMA 口径)。禁止在 indicators、models、backtest 或任何其他模块里直接调用 yf.download()。回测层尤其要遵守这一条,防止出现"指标用复权价、回测用原始价"的数据混用。